
install chainer ver.1.18.0
$ sudo pip uninstall cupy
$ sudo pip uninstall chainer
$ sudo pip install chainer=="1.18.0"

install chainer ver.2.0.0
$ sudo pip unistall chainer
$ sudo pip install cupy
$ pip install chainer
